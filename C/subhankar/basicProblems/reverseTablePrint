// print the reverse table for a number n

#include <stdio.h>

int main()
{
    int n;
    printf("enter the number: ");
    scanf("%d", &n);
    printf("the table of %d is:", n);
    for (int i = 10; i >= 1; i--)

    {
        printf("%d\n", n * i);
    }
    return 0;
}
