// print odd number 5 to n

#include <stdio.h>

int main()
{
int p;
    printf("enter the number: ");
    scanf("%d", &p);
    for (int i = 5; i <= p; i++)
    {
        if (i % 2 == 0)
        {
            continue;
        }

        printf("%d\n", i);
    }
    return 0;
}
